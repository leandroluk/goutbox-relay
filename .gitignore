# Build artefacts
bin/

# Local environment variables (may contain credentials)
# Copy .env.example to .env to get started
.env

# Coverage reports (only the SVG badge is committed)
.public/coverage.out
.public/coverage.html